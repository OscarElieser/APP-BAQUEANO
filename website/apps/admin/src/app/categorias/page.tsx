/**
 * WHY
 * Manages destination and content categories for consistent discovery.
 *
 * HOW
 * Uses the shared module scaffold until CRUD forms are attached.
 *
 * WHAT
 * Categories admin route.
 */
import { ModulePlaceholder } from "../../components/ModulePlaceholder";

export default function CategoriasPage() {
  return <ModulePlaceholder title="Categorias" collection="categories" />;
}
