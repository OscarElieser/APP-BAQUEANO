/**
 * WHY
 * Maintains department-level data used by map, filters, and Android catalog views.
 *
 * HOW
 * Uses the shared module scaffold until CRUD forms are attached.
 *
 * WHAT
 * Departments admin route.
 */
import { ModulePlaceholder } from "../../components/ModulePlaceholder";

export default function DepartamentosPage() {
  return <ModulePlaceholder title="Departamentos" collection="departments" />;
}
