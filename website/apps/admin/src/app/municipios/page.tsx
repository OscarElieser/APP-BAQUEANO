/**
 * WHY
 * Maintains municipality-level data for precise destination discovery.
 *
 * HOW
 * Uses the shared module scaffold until CRUD forms are attached.
 *
 * WHAT
 * Municipalities admin route.
 */
import { ModulePlaceholder } from "../../components/ModulePlaceholder";

export default function MunicipiosPage() {
  return <ModulePlaceholder title="Municipios" collection="municipalities" />;
}
