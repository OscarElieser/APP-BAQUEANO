/**
 * WHY
 * Gives operators a future workspace for bookings and traveler-host coordination.
 *
 * HOW
 * Uses the shared module scaffold until reservation workflows are attached.
 *
 * WHAT
 * Reservations admin route.
 */
import { ModulePlaceholder } from "../../components/ModulePlaceholder";

export default function ReservasPage() {
  return <ModulePlaceholder title="Reservas" collection="reservations" />;
}
