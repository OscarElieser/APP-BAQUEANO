/**
 * WHY
 * Lets teams manage environmental and community impact records.
 *
 * HOW
 * Uses the shared module scaffold until impact forms are attached.
 *
 * WHAT
 * Sustainability admin route.
 */
import { ModulePlaceholder } from "../../components/ModulePlaceholder";

export default function SostenibilidadAdminPage() {
  return <ModulePlaceholder title="Sostenibilidad" collection="sustainability" />;
}
