rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Fotografías de destinos y avatares de exploradores
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
